name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.85.0
      - uses: Swatinem/rust-cache@v2
      - name: Minimum supported Rust tests
        run: |
          cargo +1.85.0 test -p thekernel-linux-usercopy --all-features
          cargo +1.85.0 check -p thekernel-linux-usercopy --no-default-features --lib

  stable:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
          targets: riscv64gc-unknown-none-elf,loongarch64-unknown-none
      - uses: Swatinem/rust-cache@v2
      - name: Stable-package release-candidate gate
        env:
          CARGO_TOOLCHAIN: stable
        run: ./scripts/ci.sh

  thekernel-nightly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2025-05-20
          components: clippy,rustfmt
          targets: riscv64gc-unknown-none-elf,loongarch64-unknown-none
      - uses: Swatinem/rust-cache@v2
      - name: Full nightly release-candidate gate
        env:
          CARGO_TOOLCHAIN: nightly-2025-05-20
        run: ./scripts/ci.sh
