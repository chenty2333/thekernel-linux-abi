[package]
name = "thekernel-linux-seccomp"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Bounded classic-BPF and seccomp policy state for no_std Linux ABI kernels"
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["kernel", "linux", "no-std", "seccomp", "bpf"]
categories = ["no-std", "os"]

[features]
default = []

[package.metadata.docs.rs]
all-features = true

[package.metadata.thekernel]
toolchain = "nightly-2025-05-20"
nightly-features = ["allocator_api"]

[lints]
workspace = true
